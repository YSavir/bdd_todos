<h1><%= @todo.title %></h1>
<% if @todo.complete? %>
  <p>Todo Complete!</p>
<% else %>
  <%= form_tag toggle_todo_completeness_path(@todo), :method => 'PATCH' do %>
    <%= submit_tag('Complete Todo') %>
  <% end %>
<% end %>
